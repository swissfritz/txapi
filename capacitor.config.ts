import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'at.taxoil.app111111',
  appName: 'Taxoil',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
